/**
 * Aquafeel Design System (app-wide).
 * Canonical, neutrally-named surface for the immersive aqua aesthetic used
 * across the whole app. Re-exports the shared kit so every screen imports
 * the same primitives from one place.
 */
export {
  PortalBackground as AquaBackground,
  PortalKicker as Kicker,
  PortalTitle as DisplayTitle,
  GlassCard,
  StatTile,
  LevelMeter,
  CountUp,
  Reveal,
  MagneticButton,
} from '../ClientPortal/portalKit';
